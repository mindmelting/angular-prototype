machine:
  environment:
    CODECLIMATE_REPO_TOKEN: KigvulWHtcuCcIEppxHeMyWZDse0rxFGC
test:
  post:
    - mkdir -p $CIRCLE_TEST_REPORTS/junit/
    - find . -type f -regex "./reports/*.*xml" -exec cp {} $CIRCLE_TEST_REPORTS/junit/ \;
